package com.sobot.chat.core.http.i;

import com.sobot.chat.core.http.model.SobotProgress;

/* compiled from: ProgressListener */
public interface a<T> {
    void a(SobotProgress sobotProgress);

    void a(T t, SobotProgress sobotProgress);

    void b(SobotProgress sobotProgress);

    void c(SobotProgress sobotProgress);

    void d(SobotProgress sobotProgress);
}
